#include "main.h"

/**
 * _abs - prototype
 * Description: 'Absolute value'
 * @n: integer
 * Return: Always (n) success
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
}
