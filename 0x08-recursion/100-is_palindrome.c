#include "main.h"

/**
 * is_palindrome - prototype to print a palindrome
 * @s: string to be checked
 * Return: 1
 */

int is_palindrome(char *s)
{
	if (*s == '\0')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
