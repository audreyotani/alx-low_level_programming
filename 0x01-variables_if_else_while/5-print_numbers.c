#include <stdio.h>

/**
 * main - Entry point
 * Description: 'baseten numbers'
 * Return: Always (0) Success
 */

int main(void)
{
	int num = 0;

	while (num <= 10)
	{
		printf("%d\n", num);
		num++;
	}
	return (0);
}
