#include <stdio.h>
#include "main.h"

/**
 * main - entry point
 * @argc: function to count arguments
 * @argv: array of points to strings
 * Return: 0
 */

int main(int argc, char *argv[])
{
	void (argc);
	printf("%s\n", argv[0]);

	return (0);
}
